---
import Layout from "../layouts/Layout.astro";

import adMug from "../images/ad-mug-centered.png";
import adpharmBarn from "../images/adpharm-barn.webp";
import emberPhone from "../images/ember-phone-demo.webp";
---

<Layout title="Adpharm Xmas 2023">
  <script>
    // Import npm modules.
    import confetti from "canvas-confetti";

    // Find our component DOM on the page.
    // const buttons = document.querySelectorAll("[data-confetti-button]");

    // // Add event listeners to fire confetti when a button is clicked.
    // buttons.forEach((button) => {
    //   button.addEventListener("click", () => confetti());
    // });

    // Fire confetti when the page loads.
    // confetti({
    //   particleCount: 100,
    //   spread: 70,
    //   origin: { y: 0.6 },
    // });

    var end = Date.now() + 10 * 1000;

    // var scalar = 2;
    // var unicorn = confetti.shapeFromText({ text: "🦄", scalar });
    // "💩", "😃", "🥵", "🥶", "🤌", "👀", "🧜", "🔥", "❤️‍🔥", "💯"
    let colors = ["#df5f36", "#e37f50", "#f1bb99"];

    const defaults = {
      particleCount: 2,
      spread: 55,
      colors,
      // shapes: [unicorn],
    };

    setTimeout(function () {
      (function frame() {
        confetti({
          ...defaults,
          angle: 60,
          origin: { x: 0, y: 0.7 },
        });
        confetti({
          ...defaults,
          angle: 120,
          origin: { x: 1, y: 0.7 },
        });

        if (Date.now() < end) {
          requestAnimationFrame(frame);
        }
      })();
    }, 4900);
  </script>

  <main class="relative text-[#486055]">

    <div class="flex flex-col h-screen text-center">


    </div>

    <!-- <div
      class="mt-4 mx-auto flex items-center justify-center rounded-full h-24 aspect-square bg-gradient-to-r from-[#e1ecef] to-[#e7f2f5] animate__animated animate__fadeIn animate__slow animate__delay-2s"
    >
      <img
        src={adpharmBarn.src}
        alt="Adpharm barn"
        class="relative w-full transform scale-125 object-contain px-4 mx-auto rounded-full"
        data-confetti-button
      />
    </div> -->

    <div class="mt-12 relative w-full">
      <!-- <h1
        class="w-full text-7xl font-brolimo text-center text-[#486055] text-opacity-40 animate__animated animate__fadeIn animate__slow"
      >
        Winner!
      </h1> -->

      <div class="text-center">
        <p class="text-lg animate__animated animate__fadeIn">
          The first order of business for the newest member of the Adpharm family...
        </p>
        <p class="pt-4 animate__animated animate__fadeIn animate__slow animate__delay-2s">
          ...solving the office mug crisis.
        </p>
      </div>

      <!-- <div class="absolute top-0 left-0 translate-y-1/4 translate-x-1/4 rounded-full w-[75%] aspect-square bg-[#486055]/40"></div> -->

      <div class="w-[90%] mx-auto animate__animated animate__tada animate__slow animate__repeat-2 animate__delay-5s">
        <img
          src={adMug.src}
          alt="Adpharm mug"
          class="mt-8 relative w-full px-8 mx-auto animate__animated animate__fadeIn animate__delay-5s"
        />
        <!-- class="mt-8 relative w-full px-8 mx-auto animate__animated animate__fadeIn animate__slow" -->
      </div>
    </div>

    <div class="text-lg text-center px-4 mt-8 animate__animated animate__fadeIn animate__slow animate__delay-5s">
      <!-- <p class="text-md opacity-70">It's time to put an end to our mug shortage.</p>
      <p class="text-xl font-medium">
        Your own <span class="text-[#df5f36]">Ember Smart Mug 2</span>, courtesy of adpharm digital, will surely help.
      </p> -->

      <!-- <p class="text-lg">The first order of business for the newest member of the Adpharm family...</p>
      <p class="">solving the office mug crisis.</p> -->
      <p class="mt-4 text-xl font-medium">
        So here's a mug just for you.
        <!-- Your own <span class="text-[#df5f36]">Ember Smart Mug 2</span> will surely help. -->
      </p>

      <p class="text-sm">But not just any mug...</p>

      <div class="mt-8"></div>

      <p class="mt-4 text-sm text-gray-500 px-16">
        <a
          href="https://ember.com/products/ember-mug-2"
          target="_blank"
          rel="noreferrer"
          class="text-indigo-600 underline">Learn more about your smart mug on the Ember website.</a
        >
      </p>
    </div>

    <footer class="pt-32 pb-8 animate__animated animate__fadeIn animate__slow animate__delay-2s">
      <div
        class="mx-auto flex items-center justify-center rounded-full h-24 aspect-square bg-gradient-to-r from-[#e1ecef] to-[#e7f2f5]"
      >
        <img
          src={adpharmBarn.src}
          alt="Adpharm barn"
          class="relative w-full transform scale-125 object-contain px-4 mx-auto rounded-full"
          data-confetti-button
        />
      </div>

      <p class="mt-4 text-xs text-opacity-70 text-center">Adpharm Xmas 2023</p>
    </footer>
  </main>
</Layout>
